# Example environment variables for to_do_database container
# Copy this file to .env and adjust as needed. The scripts will also work with defaults if not provided.

# PUBLIC_INTERFACE
# DB connection configuration for MySQL service
DB_NAME=myapp
DB_USER=appuser
DB_PASSWORD=dbuser123
# Per requirements, MySQL listens on port 5001
DB_PORT=5001
